php:
  build: .
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - .\www: /var/www/html

database:
  image: mysql:5.7
  volumes:
    - /var/lib/mysql]
  environment:
    - MYSQL_ROOT_PASSWORD=root
    - MYSQL_DATABASE=123456